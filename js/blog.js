document.addEventListener('DOMContentLoaded',()=>{const header=document.querySelector('[data-nehemiah-header]'),toggle=header?.querySelector('[data-mobile-toggle]'),panel=header?.querySelector('[data-mobile-panel]');const close=()=>{if(!toggle||!panel)return;toggle.setAttribute('aria-expanded','false');panel.classList.remove('is-open');panel.hidden=true};if(toggle&&panel){toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')!=='true';toggle.setAttribute('aria-expanded',String(open));panel.hidden=!open;panel.classList.toggle('is-open',open)});panel.querySelectorAll('[data-mobile-link]').forEach(link=>link.addEventListener('click',close))}const grid=document.querySelector('[data-blog-grid]'),sort=document.querySelector('[data-blog-sort]');if(!grid||!sort)return;const cards=[...grid.querySelectorAll('.blog-card')];document.querySelector('[data-article-count]').textContent=cards.length;sort.addEventListener('change',()=>{[...cards].sort((a,b)=>sort.value==='title'?a.dataset.title.localeCompare(b.dataset.title):(sort.value==='oldest'?-1:1)*(new Date(b.dataset.date)-new Date(a.dataset.date))).forEach(card=>grid.append(card))})});
