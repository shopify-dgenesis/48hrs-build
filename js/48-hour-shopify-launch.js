document.addEventListener('DOMContentLoaded',()=>{const header=document.querySelector('[data-nehemiah-header]');const toggle=header?.querySelector('[data-mobile-toggle]');const panel=header?.querySelector('[data-mobile-panel]');const close=()=>{if(!toggle||!panel)return;toggle.setAttribute('aria-expanded','false');panel.classList.remove('is-open');panel.hidden=true};if(toggle&&panel){toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')!=='true';toggle.setAttribute('aria-expanded',String(open));panel.hidden=!open;panel.classList.toggle('is-open',open)});panel.querySelectorAll('[data-mobile-link]').forEach(link=>link.addEventListener('click',close));window.addEventListener('resize',()=>{if(innerWidth>749)close()})}const cards=[...document.querySelectorAll('.roadmap-card')];const boxes=[...document.querySelectorAll('.task-list input')];const key='nehemiah-48-hour-launch-progress';let saved=[];try{saved=JSON.parse(localStorage.getItem(key)||'[]')}catch{saved=[]}boxes.forEach((box,index)=>{box.checked=Boolean(saved[index]);box.addEventListener('change',update)});function update(){const done=boxes.filter(box=>box.checked).length;const percent=Math.round(done/boxes.length*100);document.querySelector('[data-progress-percent]').textContent=`${percent}%`;document.querySelector('[data-progress-ring]').style.background=`conic-gradient(var(--lg-lime) ${percent*3.6}deg,#21353c 0deg)`;cards.forEach(card=>{const group=card.dataset.group;const groupBoxes=[...card.querySelectorAll('input')];const groupDone=groupBoxes.filter(box=>box.checked).length;card.querySelector('[data-card-total]').textContent=`${groupDone} / ${groupBoxes.length} Completed`;document.querySelector(`[data-group-count="${group}"]`).textContent=`${groupDone} / ${groupBoxes.length}`;document.querySelector(`[data-group-bar="${group}"]`).style.width=`${groupDone/groupBoxes.length*100}%`});const text=document.querySelector('[data-readiness-text]');const badge=document.querySelector('[data-readiness-badge]');if(percent===100){text.textContent='Your store is ready to launch!';badge.textContent='Launch Ready'}else if(percent>=75){text.textContent="You're almost ready to launch.";badge.textContent='Great Progress'}else if(percent>=35){text.textContent='Keep moving through the roadmap.';badge.textContent='In Progress'}else{text.textContent='Start checking tasks below.';badge.textContent='Getting Started'}try{localStorage.setItem(key,JSON.stringify(boxes.map(box=>box.checked)))}catch{}}document.querySelector('[data-reset]')?.addEventListener('click',()=>{boxes.forEach(box=>{box.checked=false});update()});document.querySelectorAll('[data-print]').forEach(button=>button.addEventListener('click',()=>window.print()));update()});
